# Create a library called "libtrackball" which includes the source
# file "trackball.c". and "tb.c". 
# The extension is already found.  Any number of sources could be
# listed here.

ADD_LIBRARY(LibTrackball trackball tb)
